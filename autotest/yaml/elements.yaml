# Element aliases for deepin-editor
# All UI elements referenced by test cases must be registered here.
# Captured from live AT-SPI tree + source code analysis (dtextedit.cpp, window.cpp, tabbar.cpp)
app: deepin-editor

vars:
  BUILD_DIR: "${PROJECT_ROOT}/build"
  APP_PATH: "deepin-editor"
  TEST_FILES_DIR: "${PROJECT_ROOT}/test_files"

elements:
  # ===== Main window =====
  main_frame:
    name: "DMainWindow"
    role: "frame"

  # ===== Titlebar =====
  menu_button:
    name: "DTitlebarDWindowOptionButton"
    role: "push button"

  close_button:
    name: "DTitlebarDWindowCloseButton"
    role: "push button"

  minimize_button:
    name: "DTitlebarDWindowMinButton"
    role: "push button"

  maximize_button:
    name: "DTitlebarDWindowMaxButton"
    role: "push button"

  # ===== Tab bar =====
  tab_bar:
    role: "page tab list"

  add_tab_button:
    name: "DTabBarAddButton"
    role: "push button"

  # ===== Edit area (text editor) =====
  edit_area:
    role: "text"

  # Center of edit area for right-click context menus
  edit_center:
    x: 500
    y: 350

  # ===== Status bar =====
  status_row_col:
    name: "行"
    role: "label"

  status_char_count:
    name: "字数"
    role: "label"

  status_zoom:
    role: "label"

  status_insert:
    name: "插入"
    role: "label"

  status_overwrite:
    name: "覆盖"
    role: "label"

  # ===== Reload / Save As buttons (shown in modified tabs) =====
  reload_button:
    name: "重新载入"
    role: "push button"

  save_as_button:
    name: "另存为"
    role: "push button"

  # ===== Main menu items (via DTitlebarDWindowOptionButton) =====
  # Use main_menu_comb with these refs
  menu_new_window:
    menu: ["新窗口"]

  menu_new_tab:
    menu: ["新标签页"]

  menu_open_file:
    menu: ["打开文件"]

  menu_save:
    menu: ["保存"]

  menu_save_as:
    menu: ["另存为"]

  menu_print:
    menu: ["打印"]

  menu_find:
    name: "查找"
    role: "menu item"

  menu_theme:
    menu: ["切换主题"]

  menu_settings:
    menu: ["设置"]

  # ===== Context menu items (right-click in edit area) =====
  # Use context_menu_comb with these refs. Coordinates are resolved from the target element,
  # so the right-click position follows the text/tab under test instead of a hardcoded point.
  ctx_undo:
    click_target: edit_area
    menu: ["撤销"]

  ctx_redo:
    click_target: edit_area
    menu: ["重做"]

  ctx_cut:
    click_target: edit_area
    menu: ["剪切"]

  ctx_copy:
    click_target: edit_area
    menu: ["复制"]

  ctx_paste:
    click_target: edit_area
    menu: ["粘贴"]

  ctx_delete:
    click_target: edit_area
    menu: ["删除"]

  ctx_select_all:
    click_target: edit_area
    menu: ["全选"]

  ctx_find:
    click_target: edit_area
    menu: ["查找"]

  ctx_replace:
    click_target: edit_area
    menu: ["替换"]

  ctx_jump_line:
    click_target: edit_area
    menu: ["跳到行"]

  ctx_enable_readonly:
    click_target: edit_area
    menu: ["开启只读模式"]

  ctx_disable_readonly:
    click_target: edit_area
    menu: ["关闭只读模式"]

  ctx_fullscreen:
    click_target: edit_area
    menu: ["全屏"]

  ctx_exit_fullscreen:
    click_target: edit_area
    menu: ["退出全屏"]

  ctx_open_in_filemanager:
    click_target: edit_area
    menu: ["在文件管理器中显示"]

  ctx_add_comment:
    click_target: edit_area
    menu: ["添加注释"]

  ctx_remove_comment:
    click_target: edit_area
    menu: ["取消注释"]

  ctx_voice_read:
    click_target: edit_area
    menu: ["语音朗读"]

  ctx_dictation:
    click_target: edit_area
    menu: ["语音听写"]

  ctx_translate:
    click_target: edit_area
    menu: ["文本翻译"]

  ctx_column_mode:
    click_target: edit_area
    menu: ["列编辑模式"]

  # Bookmarks
  ctx_add_bookmark:
    click_target: edit_area
    menu: ["添加书签"]

  ctx_remove_bookmark:
    click_target: edit_area
    menu: ["清除书签"]

  ctx_prev_bookmark:
    click_target: edit_area
    menu: ["上一个书签"]

  ctx_next_bookmark:
    click_target: edit_area
    menu: ["下一个书签"]

  ctx_clear_all_bookmarks:
    click_target: edit_area
    menu: ["清除所有书签"]

  # Fold / Unfold
  ctx_fold_all:
    click_target: edit_area
    menu: ["折叠所有层次"]

  ctx_fold_current:
    click_target: edit_area
    menu: ["折叠当前层次"]

  ctx_unfold_all:
    click_target: edit_area
    menu: ["展开所有层次"]

  ctx_unfold_current:
    click_target: edit_area
    menu: ["展开当前层次"]

  # Color Mark submenu
  ctx_color_mark:
    click_target: edit_area
    menu: ["颜色标记"]

  ctx_mark_current:
    click_target: edit_area
    menu: ["颜色标记", "添加标记"]

  ctx_mark_all:
    click_target: edit_area
    menu: ["颜色标记", "标记所有"]

  ctx_clear_last_mark:
    click_target: edit_area
    menu: ["颜色标记", "清除上次标记"]

  ctx_clear_all_marks:
    click_target: edit_area
    menu: ["颜色标记", "清除所有标记"]

  # Change Case submenu
  ctx_upper_case:
    click_target: edit_area
    menu: ["切换大小写", "大写"]

  ctx_lower_case:
    click_target: edit_area
    menu: ["切换大小写", "小写"]

  ctx_capitalize:
    click_target: edit_area
    menu: ["切换大小写", "首字母大写"]

  # ===== Tab bar context menu (right-click on tab) =====
  tab_center:
    click_target: tab_bar

  ctx_close_tab:
    click_target: tab_bar
    menu: ["关闭标签页"]

  ctx_close_other_tabs:
    click_target: tab_bar
    menu: ["关闭其他标签页"]

  ctx_close_left_tabs:
    click_target: tab_bar
    menu: ["更多关闭方式", "关闭左侧所有标签页"]

  ctx_close_right_tabs:
    click_target: tab_bar
    menu: ["更多关闭方式", "关闭右侧所有标签页"]

  ctx_close_unmodified_tabs:
    click_target: tab_bar
    menu: ["更多关闭方式", "关闭所有未修改标签页"]

  # ===== Find / Replace dialog elements =====
  find_input:
    role: "text"
    index: 0

  replace_input:
    role: "text"
    index: 1

  find_next_button:
    name: "查找下一个"
    role: "push button"

  replace_button:
    name: "替换"
    role: "push button"

  replace_all_button:
    name: "全部替换"
    role: "push button"

  skip_button:
    name: "跳过"
    role: "push button"

  replace_remaining_button:
    name: "剩余替换"
    role: "push button"

  close_find_button:
    name: "关闭"
    role: "push button"

  # Switch from find to replace mode
  switch_to_replace_button:
    name: "替换"
    role: "push button"

  # ===== Jump to line bar =====
  jump_line_input:
    name: "跳到行"
    role: "text"

  # ===== Settings dialog =====
  settings_dialog:
    role: "dialog"

  # ===== About dialog =====
  about_dialog:
    role: "dialog"
